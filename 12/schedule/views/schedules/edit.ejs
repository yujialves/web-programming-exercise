<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta http-equiv="content-type" content="text/html; charset=UTF-8">
        <title><%= title %></title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>
    <body>
        <head>
            <h1><%= title %></h1>
        </head>
        <div role="main">
            <form method="post" action="/schedules/edit/<%= id %>">
                <table>
                    <tr>
                        <th>TITLE</th>
                        <td><input type="text" name="title" value="<%= form.title %>"></td>
                    </tr>
                    <tr>
                      <th>CONTENT</th>
                      <td><textarea name="content"><%= form.content %></textarea></td>
                    </tr>
                    <tr>
                      <th>PLACE</th>
                      <td><textarea name="place"><%= form.place %></textarea></td>
                    </tr>
                    <tr>
                      <th>DATE</th>
                      <td><input type="date" name="date" value="<%= form.date %>"></td>
                    </tr>
                    <tr>
                      <th>START</th>
                      <td><input type="time" name="start_time" value="<%= form.start_time %>" min="0:00" max="24:00" required></td>
                    </tr>
                    <tr>
                      <th>END</th>
                      <td><input type="time" name="end_time" value="<%= form.end_time %>" min="0:00" max="24:00" required></td>
                    </tr>
                    <tr>
                        <th></th>
                        <td><input type="submit" value="作成"></td>
                    </tr>
                </table>
            </form>
        </div>
    </body>
</html>
